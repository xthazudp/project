// git==> version control tool

// git command

// git --version

// git init  ==> initilized git in the project folder

// git status ==> it shows the current status of git
// branch ==> devision(seperation of features)
// commit ==> messages tracking history

// git add <file_name> or .(dot) // track all the newly added and modified file
// git commit -m 'message to be commited' // maintains version of code with commit message

// list the tracking history

// git log
// press q to exit

// copy the commit hash where we want to revert

// git checkout <commitHash>
// it will take us to the commit where the code is tracked

// git checkout -b <branch_name> to create new branch

// day by day command
// git add .
// git commit -m 'commit message'
// git push origin master

// student's end
// select a folder to clone the project
// git clone <remote address> [ONLY ONCE]
// you will get the folder with name same as it is from gitlab
// cd name_of_your_folder

// day by day action
// git pull
